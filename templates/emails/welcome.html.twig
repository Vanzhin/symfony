{% extends 'emails/base.html.twig' %}
{% block title %} Welcome to {{ app_name }} {% endblock %}
{% block body %}
    <p>Хеллоу, дорогой {{ user.name }}!</p>
    <p>Это тестовое письмо, которое отсылается после регистрации пользователя.</p>
    <p class="button">
        <a class="text-no-decoration text-cornflowerblue" href="{{ url('app_homepage') }}">на сайт</a>
    </p>
    <p class="button">
        <a class="text-no-decoration text-cornflowerblue" href="{{ url('app_account') }}">мой профиль</a>
    </p>

{% endblock %}